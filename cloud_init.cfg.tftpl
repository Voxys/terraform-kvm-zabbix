#cloud-config 
users:
  - name: ${username}
    ssh_pwauth: true
    home: /home/${username}
    ssh_authorized_keys:
    - ${public_key}
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    shell: /bin/bash
    groups: wheel

growpart:
  mode: growpart
  devices: ["/"]
  ignore_growroot_disabled: false
